[workspace]
members = [
    "crates/core-engine",
    "crates/ingest-models",
    "crates/distribution",
]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = false
rpath = false
overflow-checks = false
